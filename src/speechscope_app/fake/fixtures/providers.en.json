[
  {
    "name": "nlp",
    "version": "1.0",
    "requires": [
      "transcript"
    ],
    "params": {
      "language": {
        "default": "",
        "type": "str",
        "description": "Analysis language; empty = take it from the transcript"
      },
      "mattr_window": {
        "default": 50,
        "type": "int",
        "description": "Moving window width for MATTR and PMI (words). The legacy script derived it from the shortest transcript in the batch",
        "ge": 2
      }
    }
  },
  {
    "name": "phonemes",
    "version": "1.1",
    "requires": [],
    "params": {
      "phnrec_dir": {
        "default": "",
        "type": "str",
        "description": "Folder with phnrec.exe and its model; empty = the model store"
      },
      "min_phones": {
        "default": 5,
        "type": "int",
        "description": "Below this many recognised phonemes the output is discarded as unreliable",
        "ge": 1
      },
      "timeout": {
        "default": 600.0,
        "type": "float",
        "description": "Longest allowed recogniser run (s)",
        "gt": 0
      }
    }
  },
  {
    "name": "segments",
    "version": "2.1",
    "requires": [],
    "params": {
      "model": {
        "default": "auto",
        "type": "str",
        "description": "Segmentation source: auto (whatever is at hand), labels (finished labels only), pyannote, conformer",
        "choices": [
          "auto",
          "labels",
          "pyannote",
          "conformer"
        ]
      },
      "labels_dir": {
        "default": "",
        "type": "str",
        "description": "Folder with finished labels; empty = look next to the recording"
      },
      "labels_suffix": {
        "default": ".labels.txt",
        "type": "str",
        "description": "Suffix of the label file"
      },
      "speech_labels": {
        "default": "speech,sv,su",
        "type": "str",
        "description": "Labels treated as speech, comma-separated"
      },
      "cut_audio": {
        "default": false,
        "type": "bool",
        "description": "Also save the cut-out speech as wav into the work folder"
      },
      "model_path": {
        "default": "",
        "type": "str",
        "description": "Path to the model file; empty = weights shipped with the library (conformer) or a download from Hugging Face (pyannote)"
      },
      "hf_token": {
        "default": "",
        "type": "str",
        "description": "Hugging Face token for pyannote; empty = from the HF_TOKEN variable"
      },
      "onset": {
        "default": 0.7,
        "type": "float",
        "description": "Onset threshold of the pyannote binarisation",
        "ge": 0,
        "le": 1
      },
      "offset": {
        "default": 0.7,
        "type": "float",
        "description": "Offset threshold of the pyannote binarisation",
        "ge": 0,
        "le": 1
      },
      "min_duration_on": {
        "default": 0.0,
        "type": "float",
        "description": "Shortest speech segment (s)",
        "ge": 0
      },
      "min_duration_off": {
        "default": 0.1,
        "type": "float",
        "description": "Shortest pause (s)",
        "ge": 0
      },
      "min_breath_duration": {
        "default": 0.15,
        "type": "float",
        "description": "A shorter breath is relabelled as silence by the conformer model (s)",
        "ge": 0
      },
      "chunk_seconds": {
        "default": 30.0,
        "type": "float",
        "description": "The conformer runs in windows of this length (s); 0 = the whole recording at once, memory then grows with the square of the length",
        "ge": 0
      },
      "chunk_overlap": {
        "default": 5.0,
        "type": "float",
        "description": "Overlap of neighbouring conformer windows (s)",
        "ge": 0
      },
      "runtime": {
        "default": "auto",
        "type": "str",
        "description": "What runs the models: auto (ONNX when exported, otherwise torch), onnx (without torch), torch",
        "choices": [
          "auto",
          "onnx",
          "torch"
        ]
      },
      "onnx_device": {
        "default": "auto",
        "type": "str",
        "description": "Device for onnxruntime: auto (GPU when possible, otherwise CPU), cpu, gpu or gpu:N"
      },
      "onnx_dir": {
        "default": "",
        "type": "str",
        "description": "Folder with the ONNX models; empty = segmentation-onnx in the model store"
      }
    }
  },
  {
    "name": "transcript",
    "version": "1.0",
    "requires": [],
    "params": {
      "language": {
        "default": "cs",
        "type": "str",
        "description": "Language code of the recording; empty = let the model decide"
      },
      "model": {
        "default": "large-v3",
        "type": "str",
        "description": "Whisper model, name or path"
      },
      "device": {
        "default": "auto",
        "type": "str",
        "description": "Where to compute",
        "choices": [
          "auto",
          "cpu",
          "cuda"
        ]
      },
      "compute_type": {
        "default": "auto",
        "type": "str",
        "description": "Computation precision; auto picks the fastest one the device supports. Manual choices are e.g. float16, int8_float16 or int8"
      },
      "beam_size": {
        "default": 5,
        "type": "int",
        "description": "Beam width during decoding",
        "ge": 1
      },
      "condition_on_previous_text": {
        "default": false,
        "type": "bool",
        "description": "Give the model the previous text as context. Off prevents hallucination loops in pathological speech and is faster; on keeps coherence in clean long speech"
      },
      "word_timestamps": {
        "default": true,
        "type": "bool",
        "description": "Also return timestamps of individual words (needed for speech rate)"
      },
      "drop_repetitions": {
        "default": true,
        "type": "bool",
        "description": "Drop repeated phrases, a typical sign of Whisper hallucination"
      },
      "repetition_min_length": {
        "default": 2,
        "type": "int",
        "description": "Shortest phrase checked for repetition (words)",
        "ge": 1
      },
      "repetition_max_length": {
        "default": 10,
        "type": "int",
        "description": "Longest phrase checked for repetition (words)",
        "ge": 1
      },
      "repetition_min_count": {
        "default": 4,
        "type": "int",
        "description": "How many times a phrase must repeat to be shortened",
        "ge": 2
      }
    }
  }
]
