{
  "kind": "feature",
  "name": "acoustic.quality.cpp",
  "version": "1.1",
  "tasks": [
    "ddk",
    "monologue",
    "phonation",
    "reading",
    "story"
  ],
  "requires": [],
  "vad": {
    "supported": true,
    "required": false,
    "default": {
      "ddk": false,
      "monologue": true,
      "phonation": false,
      "reading": true,
      "story": true
    }
  },
  "description": "",
  "outputs": {
    "cpp": "Cepstral peak prominence (dB)",
    "cpps": "Cepstral peak prominence smoothed (dB)"
  },
  "params": {
    "window_length": {
      "default": 0.041,
      "type": "float",
      "description": "Window length (s)",
      "gt": 0
    },
    "window_overlap": {
      "default": 90.0,
      "type": "float",
      "description": "Window overlap (%)",
      "ge": 0,
      "lt": 100
    },
    "time_smoothing": {
      "default": 10,
      "type": "int",
      "description": "Filter order for averaging cepstra over time (CPPS only)",
      "ge": 1
    },
    "quefrency_smoothing": {
      "default": 5,
      "type": "int",
      "description": "Filter order for smoothing along quefrency (CPPS only)",
      "ge": 1
    },
    "f0_min": {
      "default": 60.0,
      "type": "float",
      "description": "Lowest F0 searched (Hz), upper quefrency bound; legacy 55",
      "gt": 0
    },
    "f0_max": {
      "default": 333.0,
      "type": "float",
      "description": "Highest F0 searched (Hz), lower quefrency bound; legacy 500",
      "gt": 0
    },
    "search_offset": {
      "default": 0.0,
      "type": "float",
      "description": "Offset of the peak search start past the lower quefrency bound (s); legacy 0.001",
      "ge": 0
    },
    "trend_min_q": {
      "default": 0.001,
      "type": "float",
      "description": "Start of the trend-line regression range (s of quefrency); 0 = the search range",
      "ge": 0
    },
    "trend_max_q": {
      "default": 0.05,
      "type": "float",
      "description": "End of the trend-line regression range (s of quefrency); 0 = the search range",
      "ge": 0
    },
    "aggregate": {
      "default": "median",
      "type": "str",
      "description": "Summary across frames: median as in the legacy script, mean as in Praat and Hillenbrand (comparable with published norms)",
      "choices": [
        "mean",
        "median"
      ]
    }
  }
}
